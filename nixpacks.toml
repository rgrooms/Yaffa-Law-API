[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_x']

[phases.install]
cmds = ['npm install --include=dev --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node dist/server.js'
